<%= turbo_stream.replace "follow_button_#{@user.id}" do %>
  <%= render "follows/button", user: @user %>
<% end %>

<%= turbo_stream.replace "followers_count_#{@user.id}" do %>
  <span id="followers_count_<%= @user.id %>"><%= @user.followers.count %></span>
<% end %>

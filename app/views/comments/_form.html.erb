<%= simple_form_for comment do |f| %>
  <%= f.input :description, label: "Write your description of the place you visited." %>
  <%= f.input :photos, as: :file, direct_upload: true, label: "Upload your photos." %>
  <%= f.submit %>

<% end %>